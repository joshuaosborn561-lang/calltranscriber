[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
# Always-on worker polling Drive (~30s) for ~60s transcript turnaround.
# cronSchedule intentionally unset.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
